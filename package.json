{
  "name": "localizations-scanner",
  "displayName": "Localization Scanner",
  "description": "Scan your codebase to detect missing localization keys and hardcoded strings. Find untranslated text and maintain consistent i18n across your project.",
  "version": "2.0.2",
  "publisher": "devharry",
  "author": {
    "name": "dev_harry",
    "email": "devharry2024@gmail.com"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Happybajwa/localizations-scanner.git"
  },
  "homepage": "https://github.com/Happybajwa/localizations-scanner#readme",
  "bugs": {
    "url": "https://github.com/Happybajwa/localizations-scanner/issues"
  },
  "keywords": [
    "localization",
    "i18n",
    "internationalization",
    "translation",
    "missing keys",
    "hardcoded strings",
    "string detection",
    "scanner",
    "linter",
    "code quality",
    "react",
    "vue",
    "typescript",
    "javascript"
  ],
  "engines": {
    "vscode": "^1.109.0"
  },
  "categories": [
    "Linters",
    "Other"
  ],
  "icon": "icon.png",
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "localizationsScanner",
          "title": "Localizations Scanner",
          "icon": "icon.svg"
        }
      ]
    },
    "views": {
      "localizationsScanner": [
        {
          "id": "localizationsScanner.missingKeys",
          "name": "Missing Keys"
        }
      ]
    },
    "commands": [
      {
        "command": "localizationsScanner.refresh",
        "title": "Refresh",
        "icon": "$(refresh)"
      },
      {
        "command": "localizationsScanner.createConfig",
        "title": "Create scan.json",
        "icon": "$(new-file)"
      },
      {
        "command": "localizationsScanner.exportDiagnostics",
        "title": "Localization Scanner: Export Diagnostics",
        "icon": "$(export)"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "localizationsScanner.refresh",
          "when": "view == localizationsScanner.missingKeys",
          "group": "navigation"
        },
        {
          "command": "localizationsScanner.createConfig",
          "when": "view == localizationsScanner.missingKeys",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.109.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "esbuild": "^0.27.3",
    "eslint": "^9.39.3",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.56.1"
  },
  "dependencies": {
    "@typescript-eslint/eslint-plugin": "^8.56.1",
    "@typescript-eslint/parser": "^8.56.1",
    "fast-glob": "^3.3.3"
  }
}
